# Running Time

## 1. 算法的三大特性

domain（合法输入）， correctness（每个合法输入给出正确结果）， resources (time + space)


> 影响resources：硬件、编程水平、编译器、语言……，且通常随着 input 变大而增长。时间和空间之间存在权衡 trade-off，时间 > 空间

---

## 2. 计算模型 (Model of Computation)

不用真实时钟时间来衡量运行时间（因为受硬件/环境影响太大），**比较增长率或实际数值即可**
> RAM模型（Random Access Model）：single processor + constant time (访问任意内存位置)

### 基本操作 (Elementary Operations)
以下操作视为"一步"（not depend on input size）：
- 赋值
- 算术运算：`+ − × /`
- 比较：`< > =`
- 逻辑运算：`and, or, not`
- 数组元素的访问/更新 `a[i]`
- 指针/引用跳转（如链表移动到下一节点）

---

## 3. 运行时间的定义 & 增长率

> **T(inp)** = 算法处理输入 `inp` 时使用  Elementary Operations 的次数

> 增长速度从慢到快：**常数 < 对数 < 线性 < 线性对数 < 平方 < 立方 < 指数**

---

## 4. 常见解题套路

### 比较两个算法在不同n下的表现
**例2**：算法A是n²，算法B是50n·lg(n)

```
n=10时：
  T_A = 10² = 100
  T_B = 50×10×lg10 = 500
  → A更快

n=10⁶时：
  T_A = (10⁶)² = 10¹²
  T_B = 50×10⁶×lg(10⁶) = 50×10⁶×6 = 3×10⁸
  → B更快
```
> 💡 **关键结论**：小规模数据 vs 大规模数据，最优算法可能不同！增长率慢的算法在数据量大时会反超。

### 两个未知常数，需要先分别求出
**例3**：T_A(n)=c_A·n·log₂n，T_B(n)=c_B·n²，已知在n=2¹⁰=1024时，A用10次，B用1次操作，求n=2²⁰时谁更快？

```
第一步：分别求c_A和c_B
  c_A × 2¹⁰ × log₂(2¹⁰) = 10  → c_A = 10/(2¹⁰×10) = 1/2¹⁰
  c_B × (2¹⁰)² = 1            → c_B = 1/2²⁰

第二步：代入n=2²⁰
  T_A(2²⁰) = (1/2¹⁰) × 2²⁰ × log₂(2²⁰) = 2¹⁰ × 20 = 20,480
  T_B(2²⁰) = (1/2²⁰) × (2²⁰)² = 2²⁰ = 1,048,576

  → A更快（20,480 << 1,048,576）
```

### 算法运行时间估算 

#### 常见运行时间速查

| 情况 | 运行时间 | 复杂度 |
|---|---|---|
| 固定语句数 | c | O(1) 常数 |
| 单层循环 | cn | O(n) 线性 |
| 变量每次翻倍(如 i←2i) | c⌈lg n⌉ | O(log n) 对数 |
| 两层独立循环 | cn² | O(n²) 平方 |
| 三层独立循环 | cn³ | O(n³) 立方 |
| 内层依赖外层(如 j←i to n) | n(n+1)/2 ≈ n²/2 | O(n²) |

---

## 解题思路口诀

1. **看语句数量** —— 固定几行 → 常数时间
2. **看循环次数** —— 循环跑几次，就乘几倍
3. **看循环变量怎么变**
   - `i ← i+1`（每次+1）→ 跑 n 次 → 线性
   - `i ← 2i`（每次翻倍）→ 跑 ⌈log₂n⌉ 次 → 对数
4. **看代码块关系**
   - 先后执行（不嵌套）→ **相加**
   - 嵌套且变量独立 → **相乘**
   - 嵌套且内层依赖外层变量 → **求和**（写出等差数列再算）
5. **有if语句** —— 分别算每个分支执行了多少次，再乘以分支里的代价，最后相加

---

## 例题速记（讲义中的关键例子）

- **Algorithm 3**：`i←2i` 直到 i≥n → 迭代次数 = ⌈lg n⌉ → **对数时间**
- **Algorithm 5/6**：n层独立循环 → O(nᵏ)（k为嵌套层数）
- **Algorithm 7**：`j←i to n` → Σ(n-i+1) = n(n+1)/2 → **平方时间**
- **Algorithm 9/10/11**：if 语句 + 嵌套循环 → 先判断 if 条件"真"了几次，再乘以对应内层循环代价，多个分支结果相加
