# Running Time

## 1. 算法的三大特性

domain（合法输入）， correctness（每个合法输入给出正确结果）， resources (time + space)


> 影响resources：硬件、编程水平、编译器、语言……，且通常随着 input 变大而增长。时间和空间之间存在权衡 trade-off，时间 > 空间

---

## 2. 计算模型 (Model of Computation)

不用真实时钟时间来衡量运行时间（因为受硬件/环境影响太大），**比较增长率或实际数值即可**
> RAM模型（Random Access Model）：single processor + constant time (访问任意内存位置)

### 基本操作 (Elementary Operations)
以下操作视为"一步"（not depend on input size）：
- 赋值
- 算术运算：`+ − × /`
- 比较：`< > =`
- 逻辑运算：`and, or, not`
- 数组元素的访问/更新 `a[i]`
- 指针/引用跳转（如链表移动到下一节点）

---

## 3. 运行时间的定义 & 增长率

> **T(inp)** = 算法处理输入 `inp` 时使用  Elementary Operations 的次数

> 增长速度从慢到快：**常数 < 对数 < 线性 < 线性对数 < 平方 < 立方 < 指数**

---

## 5. 常见解题套路

### 套路B：比较两个算法在不同n下的表现
**例2**：算法A是n²，算法B是50n·lg(n)

```
n=10时：
  T_A = 10² = 100
  T_B = 50×10×lg10 = 500
  → A更快

n=10⁶时：
  T_A = (10⁶)² = 10¹²
  T_B = 50×10⁶×lg(10⁶) = 50×10⁶×6 = 3×10⁸
  → B更快
```
> 💡 **关键结论**：小规模数据 vs 大规模数据，最优算法可能不同！增长率慢的算法在数据量大时会反超。

### 套路C：两个未知常数，需要先分别求出
**例3**：T_A(n)=c_A·n·log₂n，T_B(n)=c_B·n²，已知在n=2¹⁰=1024时，A用10次，B用1次操作，求n=2²⁰时谁更快？

```
第一步：分别求c_A和c_B
  c_A × 2¹⁰ × log₂(2¹⁰) = 10  → c_A = 10/(2¹⁰×10) = 1/2¹⁰
  c_B × (2¹⁰)² = 1            → c_B = 1/2²⁰

第二步：代入n=2²⁰
  T_A(2²⁰) = (1/2¹⁰) × 2²⁰ × log₂(2²⁰) = 2¹⁰ × 20 = 20,480
  T_B(2²⁰) = (1/2²⁰) × (2²⁰)² = 2²⁰ = 1,048,576

  → A更快（20,480 << 1,048,576）
```

---

## 6. 解题万能公式模板

遇到"给定n₁时用了X次操作，求n₂时用多少次"类型题：

1. 写出运行时间的函数形式：`T(n) = c · f(n)`
2. 代入已知条件解出常数 `c`
3. 把常数代回函数，代入目标 n 计算结果

遇到"比较两个算法哪个更快"类型题：

1. 分别代入相同的 n 计算两个 T(n)
2. 比较大小，数值小的更快
3. 注意：n较小和n较大时结论可能相反！增长率（阶数）才是决定"大规模数据下谁更快"的关键因素

---

## 7. 速记口诀

- **RAM模型**：单处理器 + 常数时间访问内存 = 分析运行时间的"标准尺子"
- **基本操作**：赋值、算术、比较、逻辑、数组访问、指针跳转 —— 都算"一步"
- **迭代通常比递归快**（fastfib线性 vs slowfib指数）
- **增长率决定长期胜负**：小数据看具体数值，大数据看阶数（增长类型）