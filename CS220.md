# Running Time

## 1. 算法的三大特性

domain（合法输入）， correctness（每个合法输入给出正确结果）， resources (time + space)


> 影响resources：硬件、编程水平、编译器、语言…… 时间 > 空间

---

## 2. 计算模型 (Model of Computation)

不用真实时间来衡量运行时间（因为受硬件/环境影响太大），**比较增长率或实际数值即可**
> RAM模型（Random Access Model）：single processor + constant time (访问任意内存位置)

### 基本操作 (Elementary Operations)
以下操作视为"一步"（not depend on input size）：
- 赋值
- 算术运算：`+ − × /`
- 比较：`< > =`
- 逻辑运算：`and, or, not`
- 数组元素的访问/更新 `a[i]`
- 指针/引用跳转（如链表移动到下一节点）

---

## 4. 常见解题套路

**关键结论**：小规模数据 vs 大规模数据，最优算法可能不同！
### 两个未知常数，需要先分别求出
**例3**：T_A(n)=c_A·n·log₂n，T_B(n)=c_B·n²，已知在n=2¹⁰=1024时，A用10次，B用1次操作，求n=2²⁰时谁更快？

```
第一步：分别求c_A和c_B
  c_A × 2¹⁰ × log₂(2¹⁰) = 10  → c_A = 10/(2¹⁰×10) = 1/2¹⁰
  c_B × (2¹⁰)² = 1            → c_B = 1/2²⁰

第二步：代入n=2²⁰
  T_A(2²⁰) = (1/2¹⁰) × 2²⁰ × log₂(2²⁰) = 2¹⁰ × 20 = 20,480
  T_B(2²⁰) = (1/2²⁰) × (2²⁰)² = 2²⁰ = 1,048,576

  → A更快（20,480 << 1,048,576）
```

## 算法运行时间估算 

| 情况 | 运行时间 | 复杂度 |
|---|---|---|
| 固定语句数 | c | O(1) 常数 |
| 单层循环 | cn | O(n) 线性 |
| 变量每次翻倍(如 i←2i) | c⌈lg n⌉ | O(log n) 对数 |
| 两层独立循环 | cn² | O(n²) 平方 |
| 三层独立循环 | cn³ | O(n³) 立方 |
| 内层依赖外层(如 j←i to n) | n(n+1)/2 ≈ n²/2 | O(n²) |

---


# 渐近符号（Asymptotic Notation）

## 1. 核心思想

- **Rule 1（保留主导项）**：只保留增长最快的那一项。
- **Rule 2（忽略常数系数）**：常数倍不影响增长类别。

---

## 2. 运行时间 & 增长速度

> **T(inp)** = 算法处理输入 `input` 时使用  Elementary Operations 的次数

```
1 ≺ log n ≺ (log n)² ≺ n ≺ n log n ≺ n² ≺ n² log n ≺ n³ ≺ ⋯ ≺ 2ⁿ ≺ n!
```
---
## 3. 三种记号定义

| 记号 | *定义（存在常数c>0, n₀∈ℕ，使得∀n>n0时：） | 直觉 | 极限判据 |
|---|---|---|---|
| **O(g(n))** 上界 | f(n) ≤ c·g(n) | g 在上 f 在下 | lim f/g < ∞ |
| **Ω(g(n))** 下界 | f(n) ≥ c·g(n) | g 在下 f 在上 | lim f/g > 0 |
| **Θ(g(n))** 紧界 | f 是 O(g(n)) 且 g 是 O(f(n)) | f 和 g 增长速度相同 | lim g/f 为正实数 |

- O(g(n)) 是一个"集合"，所有增长速度不超过 g(n) 的函数，全部都装在这个集合里。

---


## 5. 渐近符号的运算规则

- **自反性**：f 是 O(f)、Ω(f)、Θ(f)。
- **对称性**：f 是 Θ(g) ⟺ g 是 Θ(f)。
- **传递性**：
  - f=O(g), g=O(h) ⟹ f=O(h)
  - f=Ω(g), g=Ω(h) ⟹ f=Ω(h)
  - f=Θ(g), g=Θ(h) ⟹ f=Θ(h)
- **和法则**：f₁=O(g₁), f₂=O(g₂) ⟹ f₁+f₂ = O(max{g₁,g₂})
- **积法则**：f₁=O(g₁), f₂=O(g₂) ⟹ f₁·f₂ = O(g₁·g₂)
- **重要关系**：f 是 Ω(g) ⟺ g 是 O(f)。

### 换底公式
对任意 a>1：`log_a(n)` 是 `Θ(lg n)`（因为 log_a n = lg n / lg a，比值为正常数）。

---

## 4. 典型证明套路

**证明 f(n) 是 O(g(n))**：
1. 找到常数 c, n₀，使得对所有 n>n₀ 有 f(n) ≤ c·g(n)；或
2. 计算极限 lim(n→∞) f(n)/g(n)，若 < ∞ 则成立。

**证明 f(n) 不是 O(g(n))**（反证法）：
假设存在 c, n₀ 使 f(n) ≤ c·g(n) 恒成立 → 推出矛盾。

**证明 f(n) 是 Θ(g(n))**：
需要同时找 c₁, c₂, n₀，使 c₁g(n) ≤ f(n) ≤ c₂g(n)；或证明极限为正实数。

---
# Algorithm Analysis 

---

## 1. 基本操作（Elementary Operations）

- **现实里超出 machine word 时**：数字要存成数组（每个 bit 一格），此时加法不再是 O(1)

### 大数加法算法
```
function ADDITION(a[0..n-1], b[0..n-1])
    初始化数组sum s[0..n] 
    c ← 0                     // 进位
    for j ← 0 to n-1:
        s[j] ← a[j]+b[j]+c，并更新进位 c
    若最后 c = 1，则 s[n] = 1
    return s
```
- 运行时间属于 **Θ(n)**（n 是数字的位数，不是数字的值）

- **但是本课程默认**：仍把 `+`、`×` 当作基本操作，除非题目特别说明。

---

## 2. Input Size

> **定义**：输入规模 = 存储输入所需的 **比特数**，不是输入的数值！
- **x input value**（具体数值）
- **n input size**（2进制 bit数），不是输入的值。

---

## 3. 同一规模下的不同输入（Worst / Average / Best Case）

固定规模 n 下可能有很多种输入值。
- 例：n=3 → 二进制 100,101,110,111（对应值 4,5,6,7）

### 三种定义
| 类型 | 含义 |
|---|---|
| **最坏情况 Worst-case** | 规模为 n 的所有输入中，运行时间的**最大值** |
| **平均情况 Average-case** | 规模为 n 的所有输入的运行时间**平均值** |
| **最好情况 Best-case** | 规模为 n 的所有输入中，运行时间的**最小值** |


📌 **本课程**：主要做**最坏情况**分析，平均情况只在特殊算法中讨论。
---

# Selection Sort

- **输入**：无序列表 `a[0..n-1]`
- **输出**：包含相同元素的有序列表
- 只用 `<`、`>`、`=` 比较，不依赖具体数据结构如桶排序

## 2. 基础操作与时间复杂度

| 操作 | 说明 | 数组中耗时 | 链表中耗时 |
|---|---|---|---|
| comparison | 比较两个key的大小 | Θ(1) | Θ(1) |
| swap | 用临时变量交换 a[i] 和 a[j] | Θ(1) | Θ(1) |
| insert-delete | 在位置j插入、位置i删除 | O(n) | Θ(1) |

## 3. 算法性质定义

- **原地算法 (in-place)**：只用固定的 extra space（与n无关），不创建辅助数据结构

- **稳定算法 (stable)**：相同元素排序后 **相对顺序** 不变

## 4. 选择排序核心思想

1. 把列表分成 **已排序** 和 **未排序** 两部分
2. 一开始已排序部分为空，未排序部分是整个列表
3. 在未排序部分**顺序扫描**找到最大元素
4. 把最大元素和未排序的最后元素交换
5. 若未排序部分为空则结束，否则回到第3步

## 5. 伪代码

```
function SELSORT(list a[0..n-1])
    for i ← n-1 to 0 do
        k ← FINDMAX(a[0..i])      // 找a[0..i]中最大值下标
        if k ≠ i then
            SWAP(a, i, k)
    return a

function FINDMAX(list a[0..i])
    max ← 0
    for ℓ ← 1 to i do
        if (a[max] < a[ℓ]) then max ← ℓ   // 比较
    return max
```

## 6. 复杂度分析

- **比较次数**：`FINDMAX(a[0..i])` 做 i 次比较
  → 总比较次数 = (n-1)+(n-2)+...+1 = **Θ(n²)**
> ⚠️ **重点**：无论最好/最坏/平均情况，选择排序的比较次数都是 **Θ(n²)**，因为 FINDMAX 每次都要扫描整个未排序部分（与数据是否有序无关）。

| 情况 | 比较次数 | 交换次数 |
|---|---|---|
| 最好情况（已排序） | Θ(n²) | 0 |
| 最坏情况（每轮最大值都不在位置i上，如[2,3,4,5,1]） | Θ(n²) | n-1 |
| 平均情况 | Θ(n²) | 介于中间 |



## 7. 性质总结

| 性质 | 选择排序 | 说明 |
|---|---|---|
| in-place？ | ✅ 是 | 只用固定数量变量做swap和findmax |
| stable？ | ❌ 否 | 反例：[21, 14, 21, 25] → [14, 21, 21, 25]，两个21相对顺序改变 |

## 8. 在链表上运行选择排序

- 比较次数：仍是 Θ(n²)
- 不需要swap，改用 insert-delete（链表中 O(1)）
- 但仍需 Θ(n) 次 insert-delete 操作
- **总时间仍为 Θ(n²)**



