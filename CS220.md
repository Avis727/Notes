# Running Time

## 1. 算法的三大特性

domain（合法输入）， correctness（每个合法输入给出正确结果）， resources (time + space)


> 影响resources：硬件、编程水平、编译器、语言……，且通常随着 input 变大而增长。时间和空间之间存在权衡 trade-off，时间 > 空间

---

## 2. 计算模型 (Model of Computation)

不用真实时钟时间来衡量运行时间（因为受硬件/环境影响太大），**比较增长率或实际数值即可**
> RAM模型（Random Access Model）：single processor + constant time (访问任意内存位置)

### 基本操作 (Elementary Operations)
以下操作视为"一步"（not depend on input size）：
- 赋值
- 算术运算：`+ − × /`
- 比较：`< > =`
- 逻辑运算：`and, or, not`
- 数组元素的访问/更新 `a[i]`
- 指针/引用跳转（如链表移动到下一节点）

---

## 4. 常见解题套路

**关键结论**：小规模数据 vs 大规模数据，最优算法可能不同！
### 两个未知常数，需要先分别求出
**例3**：T_A(n)=c_A·n·log₂n，T_B(n)=c_B·n²，已知在n=2¹⁰=1024时，A用10次，B用1次操作，求n=2²⁰时谁更快？

```
第一步：分别求c_A和c_B
  c_A × 2¹⁰ × log₂(2¹⁰) = 10  → c_A = 10/(2¹⁰×10) = 1/2¹⁰
  c_B × (2¹⁰)² = 1            → c_B = 1/2²⁰

第二步：代入n=2²⁰
  T_A(2²⁰) = (1/2¹⁰) × 2²⁰ × log₂(2²⁰) = 2¹⁰ × 20 = 20,480
  T_B(2²⁰) = (1/2²⁰) × (2²⁰)² = 2²⁰ = 1,048,576

  → A更快（20,480 << 1,048,576）
```

## 算法运行时间估算 

| 情况 | 运行时间 | 复杂度 |
|---|---|---|
| 固定语句数 | c | O(1) 常数 |
| 单层循环 | cn | O(n) 线性 |
| 变量每次翻倍(如 i←2i) | c⌈lg n⌉ | O(log n) 对数 |
| 两层独立循环 | cn² | O(n²) 平方 |
| 三层独立循环 | cn³ | O(n³) 立方 |
| 内层依赖外层(如 j←i to n) | n(n+1)/2 ≈ n²/2 | O(n²) |

---


# 渐近符号（Asymptotic Notation）

## 1. 核心思想

**Rule 1（保留主导项）**：只保留增长最快的那一项。
**Rule 2（忽略常数系数）**：常数倍不影响增长类别。

---

## 2. 运行时间 & 增长速度

> **T(inp)** = 算法处理输入 `input` 时使用  Elementary Operations 的次数

> 增长速度从慢到快：**常数 < 对数 < 线性 < 线性对数 < 平方 < 立方 < 指数**
```
1 ≺ log n ≺ (log n)² ≺ n ≺ n log n ≺ n² ≺ n² log n ≺ n³ ≺ ⋯ ≺ 2ⁿ ≺ n!
```
---
## 3. 三种记号定义

| 记号 | *定义（存在常数c>0, n₀∈ℕ，使得∀n>n0时：） | 直觉 | 极限判据 |
|---|---|---|---|
| **O(g(n))** 上界 | f(n) ≤ c·g(n) | f 在下 g 在上 | lim f/g < ∞ |
| **Ω(g(n))** 下界 | f(n) ≥ c·g(n) | f 在上 g 在下 | lim f/g > 0 |
| **Θ(g(n))** 紧界 | f 是 O(g(n)) 且 g 是 O(f(n)) | f 和 g 增长速度相同 | lim g/f 为正实数 |

**重要关系**：f 是 Ω(g) ⟺ g 是 O(f)。
- O(g(n)) 其实是一个"集合"，所有增长速度不超过 g(n) 的函数，全部都装在这个集合里。

---

## 4. 典型证明套路

**证明 f(n) 是 O(g(n))**：
1. 找到常数 c, n₀，使得对所有 n>n₀ 有 f(n) ≤ c·g(n)；或
2. 计算极限 lim(n→∞) f(n)/g(n)，若 < ∞ 则成立。

**证明 f(n) 不是 O(g(n))**（反证法）：
假设存在 c, n₀ 使 f(n) ≤ c·g(n) 恒成立 → 推出矛盾。

**证明 f(n) 是 Θ(g(n))**：
需要同时找 c₁, c₂, n₀，使 c₁g(n) ≤ f(n) ≤ c₂g(n)；或证明极限为正实数。

---

## 5. 渐近符号的运算规则

- **自反性**：f 是 O(f)、Ω(f)、Θ(f)。
- **对称性**：f 是 Θ(g) ⟺ g 是 Θ(f)。
- **传递性**：
  - f=O(g), g=O(h) ⟹ f=O(h)
  - f=Ω(g), g=Ω(h) ⟹ f=Ω(h)
  - f=Θ(g), g=Θ(h) ⟹ f=Θ(h)
- **和法则**：f₁=O(g₁), f₂=O(g₂) ⟹ f₁+f₂ = O(max{g₁,g₂})
- **积法则**：f₁=O(g₁), f₂=O(g₂) ⟹ f₁·f₂ = O(g₁·g₂)

### 换底公式
对任意 a>1：`log_a(n)` 是 `Θ(lg n)`（因为 log_a n = lg n / lg a，比值为正常数）。

---

## 6. 支配关系 ↔ 渐近符号 对照

| 记号 | 含义 |
|---|---|
| f ∈ O(g) | f 不比 g 快 → f 与 g 同类，或被 g 支配 |
| f ∈ Ω(g) | f 不比 g 慢 → f 与 g 同类，或支配 g |
| f ∈ Θ(g) | f 与 g 增长速度完全相同（同一增长类） |

---

## 7. 课件例题解析

**Q1.** f₁=100n², f₂=n+100, f₃=100+n², f₄=log₂(n⁴)=4log₂n, f₅=n·log₂n。
哪些函数彼此渐近等价（互为 Θ）？

- f₁, f₃ 都是 Θ(n²) → **等价**
- f₂ 是 Θ(n)，f₄ 是 Θ(log n)，f₅ 是 Θ(n log n) → 三者互不相同，也不等于 f₁/f₃

✅ **答案：B（f₁ 和 f₃）**

**Q2.** 已知 f=O(g)，g=O(h)，h=Ω(f)。哪个说法最准确？

- 由传递性：f=O(g), g=O(h) ⟹ **f=O(h)**（已确定成立）
- h=Ω(f) 只说明 h 不比 f 慢，并不能推出 f=Ω(h)（即不能保证 f、h 同阶）

✅ **答案：D（f 是 O(h)）**

---

## 8. 快速判断技巧

1. 写出 f(n)/g(n) 的极限：
   - 极限 = 0 → f 是 O(g) 但**不是** Ω(g)（f 严格慢）
   - 极限 = 正常数 → f 是 Θ(g)
   - 极限 = ∞ → f 是 Ω(g) 但**不是** O(g)（f 严格快）
2. 多项式比较：只看**最高次项**。
3. 指数 vs 多项式：指数永远赢（2ⁿ 支配任意 n^k）。
4. 多项式 vs 对数：多项式赢（n 支配 (log n)^k）。
5. 阶乘 vs 指数：阶乘赢（n! 支配 2ⁿ）。
